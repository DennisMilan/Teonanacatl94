rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all direct access from the client side since all data management
    // is safely handled by the secure Express backend using firebase-admin.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
